# FastAPI backend
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3
requests>=2.31.0

# Audio processing
pydub==0.25.1
pandas==2.1.4

# PyTorch, speechbrain, pytorch-lightning are installed in Dockerfile
# to ensure correct CPU version and dependency order

# Additional dependencies for pyannote (non-torch packages only)
asteroid-filterbanks>=0.4.0
pyannote.core>=5.0.0
pyannote.database>=5.0.0
pyannote.metrics>=3.2.0

# Voice cloning (F5-TTS)
f5-tts>=1.0.0
faster-whisper>=1.0.0
soundfile>=0.12.0
