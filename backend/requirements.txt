# FastAPI backend
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
pydantic==2.5.3

# Audio processing
pydub==0.25.1
pandas==2.1.4

# PyTorch and ML dependencies are installed in Dockerfile
# to ensure correct CPU version and dependency order

# Additional dependencies for pyannote
asteroid-filterbanks>=0.4.0
pytorch-lightning>=2.0.0
pyannote.core>=5.0.0
pyannote.database>=5.0.0
pyannote.metrics>=3.2.0
speechbrain>=0.5.0
